language: ruby
cache: bundler
sudo: false
rvm:
  - 2.2.0
env:
  - DB=sqlite
  - DB=postgres
  - DB=mysql
before_script:
  - "echo \"LedgrApp::Application.config.secret_token = '$(bundle exec rake secret)'\" > config/initializers/secret_token.rb"
  - "echo \"LedgrApp::Application.config.secret_key_base = '$(bundle exec rake secret)'\" > config/initializers/secret_token.rb"
  - "echo \"Devise.secret_key = '$(bundle exec rake secret)'\" >> config/initializers/secret_token.rb"
  - psql -c 'create database ledgr_test' -U postgres
  - mysql -e 'create database ledgr_test;'
script:
  - RAILS_ENV=test bundle exec rake db:migrate test
