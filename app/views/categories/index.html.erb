<table>
	
<% @categories.each do |category| %>
  <tr>
    <td><%=  %></td>
    <td><%= link_to category.name, user_category_path(category.user, category) %></td>
    <td><%= link_to 'Destroy', [category.user, category], method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_user_category_path %>
