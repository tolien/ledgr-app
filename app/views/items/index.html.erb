<table class="table table-striped">

<% @items.each do |item| %>
  <tr>
    <td><%= link_to item.name, user_item_path(item.user, item) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Item', new_user_item_path(@user) %>
