<%= form_for([@user, @item]) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <% if @item.category_ids.count > 0 %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
		<%= f.text_field :category_ids[0] %>
  </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= @item.categories %>
