<%= form_for([@user, @entry]) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item %><br />
    <%= f.collection_select(:item_id, Item.where("user_id = ?", @user.id), :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.text_field :quantity %>
  </div>
  <div class="field">
    <%= f.label :datetime %><br />
    <%= f.text_field :datetime, type: :datetime %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
